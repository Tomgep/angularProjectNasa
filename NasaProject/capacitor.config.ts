import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'nasa-project',
  webDir: 'dist/nasa-project',
  bundledWebRuntime: false
};

export default config;
